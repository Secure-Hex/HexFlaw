# Core runtime — necesario para el slice vertical (init / ingest / analyze).
typer>=0.12.0
rich>=13.7.0
pydantic>=2.6.0
anthropic>=0.40.0
structlog>=24.1.0
defusedxml>=0.7.1

# Backends opcionales (ver pyproject.toml [project.optional-dependencies]):
#   pip install -e ".[embeddings,treesitter,pdf,secrets,dev]"
